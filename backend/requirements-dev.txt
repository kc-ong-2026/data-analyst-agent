# Development dependencies
# Linting and formatting
ruff==0.8.4  # Stable version with UP042 rule
black==24.2.0
mypy==1.8.0

# Testing (also in requirements.txt but listed here for clarity)
pytest==8.0.0
pytest-asyncio==0.23.5
pytest-cov==4.1.0
pytest-mock==3.12.0
pytest-timeout==2.2.0
